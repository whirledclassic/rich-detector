services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      PORT: 3000
    volumes:
      - rd-data:/app/data
    restart: unless-stopped

volumes:
  rd-data:
