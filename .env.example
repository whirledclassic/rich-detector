PORT=3000
PUBLIC_URL=http://localhost:3000
SESSION_SECRET=change-me-to-a-long-random-string
ADMIN_KEY=change-me-admin-key

# Leave Stripe blank to run DEMO MODE (fake payments, no cards).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=

# Optional SMTP for dethrone mail (leave blank to log emails instead)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=throne@localhost
